
  # Brand-compliant messaging system

  This is a code bundle for Brand-compliant messaging system. The original project is available at https://www.figma.com/design/Cpu4nGRSqlaZeBhE1wINKa/Brand-compliant-messaging-system.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  